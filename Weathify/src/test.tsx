import {Card} from "@nextui-org/react";

<Card for={'file-input'} className={'w-28 border-0 shadow-none bg-gray-700 hover:bg-gray-900'}>
    <input name="file-input" onChange={f} value={values} id="file-input" type={'file'} className={'w-28 opacity-0'}/>
</Card>
